{
  "name": "@nightowl/bot",
  "version": "2.0.0-alpha.1",
  "description": "NightOwl Friend Lock bot — Cloudflare Worker that brokers Telegram <-> desktop password delegation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "replay": "node --experimental-strip-types tools/replay.ts",
    "test": "echo 'no bot tests yet — covered by integration tests'"
  },
  "dependencies": {
    "@noble/ed25519": "^2.1.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240903.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.3.3",
    "wrangler": "^3.78.0"
  }
}
